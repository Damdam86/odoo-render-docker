[options]
# Configuration automatique basée sur les variables d'environnement
db_host = dpg-d1ja0dumcj7s73ae9nmg-a.oregon-postgres.render.com
db_port = 5432
db_user = odoo_w4ch_h23r_user
db_password = mXmHpz5iTs8EH83v4dghX8HjQw4HYEUX
db_sslmode = require

# Configuration serveur web (CRITIQUE pour Render)
http_port = 8069
http_interface = 0.0.0.0
workers = 0

# Configuration logs (optimisé pour Render)
log_level = info
log_handler = :INFO
logfile = False

# Configuration addons
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons

# Configuration base de données
list_db = True
db_template = template0

# Configuration performance (optimisée pour plan gratuit Render)
limit_memory_hard = 536870912
limit_memory_soft = 402653184
limit_request = 8192
limit_time_cpu = 300
limit_time_real = 600
limit_time_real_cron = -1
max_cron_threads = 1

# Configuration sécurité
admin_passwd = admin123

# Configuration sessions
session_dir = /tmp/odoo_sessions

# Configuration proxy (pour Render)
proxy_mode = True

# Configuration email (désactivé)
email_from = False
smtp_server = localhost
smtp_port = 587
smtp_user = False
smtp_password = False
smtp_ssl = False

# Configuration serveur (forcer l'écoute sur toutes les interfaces)
xmlrpc = True
xmlrpc_interface = 0.0.0.0
xmlrpc_port = 8069
